
export const UserCard = ({ title, firstName, lastName }) => {
    
    return (
        <div>
            <p>{title}</p>
            <p>{firstName}</p>
            <p>{lastName}</p>
        </div>
    )
}