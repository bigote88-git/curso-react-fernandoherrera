import { fetchGift } from "../../src/helpers/getGifts"

describe('Testing the helper getGifts.js', ()=>{
    
    test('Test the count of element of getGifts', async ()=>{
        //const gifs = await fetchGift('react native')
        
        //expect(gifs.length).toBeGreaterThan(0)
        // expect(gifs[0]).toEqual({
        //     id: expect.any(String),
        //     title: expect.any(String),
        //     url: expect.any(String)
        // })
    })
})